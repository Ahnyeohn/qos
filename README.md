# qos

this is end-to-end system for qos framework

qos includes loadbalancer(haproxy based), k8s setting, mediasoup, metric collector, resource controller(aware based)

if you curious about k8s setting and mediasoup setting, please refer network.
